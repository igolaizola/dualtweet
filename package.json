{
    "name": "netlify-cors-proxy",
    "version": "1.0.0",
    "description": "Netlify function to proxy requests to Typefully API",
    "main": "index.html",
    "scripts": {
        "build": "echo 'No build step required' && exit 0"
    },
    "dependencies": {
        "node-fetch": "^3.0.0"
    },
    "type": "module"
}
